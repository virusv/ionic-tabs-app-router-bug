import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-tabs-app-router-bug',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
